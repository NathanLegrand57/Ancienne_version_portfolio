gsap.fromTo(".navbar", {y: -200}, {
    y: 0,
    duration: 2,
    delay: 0.2,
    ease: "back.out(1.7)"
})

gsap.fromTo(".container3 h1, .container3 p", {y : -300}, {
    y : 0,
    duration: 2,
    ease: "back.out(1.7)"
})
